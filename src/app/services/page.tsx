import { Catalog } from '@neon/widgets';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Services',
};

const Services = () => {
  return <Catalog />;
};

export default Services;
